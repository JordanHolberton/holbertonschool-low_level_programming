#include "main.h"
/**
 * _abs - Script for compute the absolute value of an integrer
 * @n: integer to be computed
 *
 * Return: Absolute value negativ or positiv
 */
int _abs(int n)
{
	if (n < 0)
		return (-n);
	else
		return (n);
}
